TODO List
=========

PostCompiler:
	Test new palette.
	Fix graphic program & sprite converter to use it.
	Generalise to more sensible systems
	Update Graphic converter.

Notes:	

	Compiler auto optimise for +/-1 *2/2 *8/8 etc ?? (in base class)

?	object:method() => method(object,xxx) ?

run : $(PRGFILE)
	..$(S)bin$(S)x16emu.exe -debug -prg $(PRGFILE) -scale 2 -run



